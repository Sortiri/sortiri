/** Full-bleed within the page shell — matches sortiri-content landing separators. */
export function LandingSectionSeparator() {
  return (
    <div className="-mx-6 border-t border-[var(--landing-grid-line)] sm:-mx-8" />
  );
}
