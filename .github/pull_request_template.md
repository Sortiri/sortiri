## Summary

<!-- What changed and why? Keep it focused on agent workflows, CLI, MCP, or local timeline. -->

## Type of change

- [ ] Bug fix
- [ ] Feature
- [ ] Documentation
- [ ] Refactor / chore
- [ ] CI / tooling

## Checklist

- [ ] `npm run typecheck` passes
- [ ] `npm run lint` passes
- [ ] `npm run test:unit` passes
- [ ] Docs updated (README, CLI help, or examples) when behavior changed
- [ ] No secrets, API keys, or `.env` values committed
- [ ] Local-first: changes work without Sortiri Cloud when applicable
- [ ] No landing / marketing page changes (`src/app/page.tsx`, `landing.css`, `pricing/`, etc.)

## Agent / CLI validation

<!-- Optional: commands you ran, e.g. sortiri doctor, sortiri record, sortiri export -->

```bash
# sortiri run -- npm run test:unit
```

## Screenshots / artifacts

<!-- If UI changed in platform pages only (not landing), attach screenshots. -->
